name = "massage-shop-backend"
main = "src/index.js"
compatibility_date = "2024-03-08"

# 학생 여러분: wrangler.toml은 Cloudflare Workers의 환경 설정 파일입니다.
# 여기에는 데이터베이스 연결 문자열(Environment Variables) 등이 포함됩니다.

[vars]
# NeonDB / Supabase 연결 정보 (실제 연동 시 여기에 입력)
# DB_URL = "postgres://user:password@neondb-hostname/dbname"
