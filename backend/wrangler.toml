name = "massage-shop-backend"
main = "src/index.ts"
compatibility_date = "2024-03-08"

# 👨‍🏫 Cloudflare 통합 배포 설정
# massageshop.com 도메인의 /api/* 경로로 오는 모든 요청을 이 Worker가 처리합니다.
# (실제 도메인 연결 시 아래 주석을 해제하거나 대시보드에서 설정하세요)
# routes = [
#   { pattern = "massageshop.com/api/*", zone_name = "massageshop.com" }
# ]

[vars]
# NeonDB / Supabase 연결 정보 (Cloudflare Dashboard Secrets에서 관리 권장)
# DATABASE_URL = "..."
# SUPABASE_JWT_SECRET = "..."
